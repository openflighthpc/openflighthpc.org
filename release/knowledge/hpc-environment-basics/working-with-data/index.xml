<?xml version="1.0" encoding="utf-8" standalone="yes"?>
<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/">
  <channel>
    <title>OpenFlightHPC</title>
    <link>https://openflighthpc.org/knowledge/hpc-environment-basics/working-with-data/</link>
    <description>Recent content on OpenFlightHPC</description>
    <generator>Hugo -- gohugo.io</generator>
    <language>en-gb</language>
    <atom:link href="https://openflighthpc.org/knowledge/hpc-environment-basics/working-with-data/index.xml" rel="self" type="application/rss+xml" />
    <item>
      <title>Storage Types</title>
      <link>https://openflighthpc.org/knowledge/hpc-environment-basics/working-with-data/storage-types/</link>
      <pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate>
      <guid>https://openflighthpc.org/knowledge/hpc-environment-basics/working-with-data/storage-types/</guid>
      <description>&lt;h2 id=&#34;shared-storage&#34;&gt;Shared Storage&lt;/h2&gt;
&lt;p&gt;With HPC environments typically combining multiple hosts there comes the requirement to be able to share user data quickly between the available resources. Due to this, HPC environments usually have some sort of shared storage solution.&lt;/p&gt;
&lt;p&gt;The shared storage solution can share user home directories between all the nodes, create shared mount-points for a project or department and even provide redundancy by using resilient storage back-ends.&lt;/p&gt;
&lt;h2 id=&#34;local-scratch&#34;&gt;Local Scratch&lt;/h2&gt;
&lt;p&gt;Your compute nodes may have an amount of disk space available to store temporary data under the &lt;code&gt;/tmp&lt;/code&gt; mount-point. This area is intended for temporary data created during compute jobs, and shouldn&amp;rsquo;t be used for long-term data storage. Compute nodes are configured to clear up temporary space automatically, removing orphan data left behind by jobs.&lt;/p&gt;</description>
      <content:encoded><![CDATA[<h2 id="shared-storage">Shared Storage</h2>
<p>With HPC environments typically combining multiple hosts there comes the requirement to be able to share user data quickly between the available resources. Due to this, HPC environments usually have some sort of shared storage solution.</p>
<p>The shared storage solution can share user home directories between all the nodes, create shared mount-points for a project or department and even provide redundancy by using resilient storage back-ends.</p>
<h2 id="local-scratch">Local Scratch</h2>
<p>Your compute nodes may have an amount of disk space available to store temporary data under the <code>/tmp</code> mount-point. This area is intended for temporary data created during compute jobs, and shouldn&rsquo;t be used for long-term data storage. Compute nodes are configured to clear up temporary space automatically, removing orphan data left behind by jobs.</p>
<p>Users must make sure that they copy data they want to keep back to the shared filesystem after compute jobs have been completed.</p>
<p>Utilising local scratch can dramatically increase the execution speed of HPC workflows that generate a lot of data during runtime where a lot of it isn&rsquo;t required for analysing results. By utilising the local scratch, this data can typically be written to, and read from, quicker than network shared storage.</p>
]]></content:encoded>
    </item>
    <item>
      <title>Your Home Directory</title>
      <link>https://openflighthpc.org/knowledge/hpc-environment-basics/working-with-data/your-home-directory/</link>
      <pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate>
      <guid>https://openflighthpc.org/knowledge/hpc-environment-basics/working-with-data/your-home-directory/</guid>
      <description>&lt;p&gt;Linux automatically places users in their home-directory when they login to a node. By default, Linux will create your home-directory under the &lt;code&gt;/home/&lt;/code&gt; directory. However, on some systems the home directory location may differ.&lt;/p&gt;
&lt;p&gt;The Linux command line will accept the &lt;code&gt;~&lt;/code&gt; (tilde) symbol as a substitute for the currently logged-in user&amp;rsquo;s home-directory. The environment variable &lt;code&gt;$HOME&lt;/code&gt; is also set to this value by default. Hence, the following three commands are all equivalent when logged in as a user called &lt;code&gt;flight&lt;/code&gt;:&lt;/p&gt;</description>
      <content:encoded><![CDATA[<p>Linux automatically places users in their home-directory when they login to a node. By default, Linux will create your home-directory under the <code>/home/</code> directory. However, on some systems the home directory location may differ.</p>
<p>The Linux command line will accept the <code>~</code> (tilde) symbol as a substitute for the currently logged-in user&rsquo;s home-directory. The environment variable <code>$HOME</code> is also set to this value by default. Hence, the following three commands are all equivalent when logged in as a user called <code>flight</code>:</p>
<ul>
<li><code>ls /home/flight</code></li>
<li><code>ls ~</code></li>
<li><code>ls $HOME</code></li>
</ul>
<p>The root user in Linux has special meaning as a privileged user, and usually does not have a shared home-directory across a research environment. The root account on all nodes is likely to have a home-directory in <code>/root</code>, which is separate for every node. For security reasons, users are not permitted to login to a node as the root.</p>
]]></content:encoded>
    </item>
    <item>
      <title>Copying Data Between Hosts</title>
      <link>https://openflighthpc.org/knowledge/hpc-environment-basics/working-with-data/copy-between-hosts/</link>
      <pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate>
      <guid>https://openflighthpc.org/knowledge/hpc-environment-basics/working-with-data/copy-between-hosts/</guid>
      <description>&lt;p&gt;In a HPC environment there is likely to be a &lt;a href=&#34;https://openflighthpc.org/knowledge/hpc-environment-basics/working-with-data/storage-types/#shared-storage&#34;&gt;shared filesystem&lt;/a&gt;, so it is not normally necessary to copy data directly between nodes in the research environment. Users simply need to place the data to be shared in their home-directory on the login node, and it will be available on all compute nodes in the same location.&lt;/p&gt;
&lt;p&gt;If necessary, users can use the &lt;code&gt;scp&lt;/code&gt; command to copy files between nodes. For example, the following will copy the file &lt;code&gt;myfile.txt&lt;/code&gt; stored under &lt;code&gt;/tmp/&lt;/code&gt; on &lt;code&gt;node01&lt;/code&gt; to the &lt;code&gt;cwd&lt;/code&gt; (&lt;code&gt;.&lt;/code&gt;) of this host:&lt;/p&gt;</description>
      <content:encoded><![CDATA[<p>In a HPC environment there is likely to be a <a href="/knowledge/hpc-environment-basics/working-with-data/storage-types/#shared-storage">shared filesystem</a>, so it is not normally necessary to copy data directly between nodes in the research environment. Users simply need to place the data to be shared in their home-directory on the login node, and it will be available on all compute nodes in the same location.</p>
<p>If necessary, users can use the <code>scp</code> command to copy files between nodes. For example, the following will copy the file <code>myfile.txt</code> stored under <code>/tmp/</code> on <code>node01</code> to the <code>cwd</code> (<code>.</code>) of this host:</p>






<div class="highlight"><pre tabindex="0" style="color:#f8f8f2;background-color:#272822;-moz-tab-size:4;-o-tab-size:4;tab-size:4;"><code class="language-bash" data-lang="bash"><span style="display:flex;"><span>scp node01:/tmp/myfile.txt .</span></span></code></pre></div>
<p>Alternatively, users could login to a host that has unshared data on and copy the data back to the shared filesystem.</p>
]]></content:encoded>
    </item>
  </channel>
</rss>
